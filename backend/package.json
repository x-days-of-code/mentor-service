{
  "name": "@mentor-service/backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "ts-node src/index.ts",
    "watch": "nodemon",
    "build": "tsc",
    "test": "exit 0",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "db": "docker run -it --expose=${DB_PORT} -d mysql"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "dotenv": "^8.0.0",
    "nodemon": "^1.19.1",
    "ts-node": "^8.3.0",
    "tslint": "^5.18.0",
    "typescript": "^3.5.2"
  },
  "nodemonConfig": {
    "watch": [
      "src/**/*.ts"
    ],
    "ignore": [
      "src/**/*.spec.ts"
    ],
    "exec": "npm start"
  },
  "devDependencies": {
    "@types/express": "^4.17.0"
  }
}
